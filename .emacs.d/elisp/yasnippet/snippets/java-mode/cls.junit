#name : junit class
# --
/** \brief UnitTest for `(substring (file-name-sans-extension (buffer-name)) 0 -4)` **/

import static org.junit.Assert.*;
import org.junit.*;

/**
 * 
 */
public class ${1:`(file-name-sans-extension (buffer-name))`} {

    /**
     * 
     */
    @Test public void test${2:functionality to test}() {
      $0
    }
}